import { combineReducers } from "redux";

const defaultinfo = {};

function imginfo(state = defaultinfo, action) {
  switch (action.type) {
    case 1:
      break;

    default:
      break;
  }

  return defaultinfo;
}

export default imginfo;
